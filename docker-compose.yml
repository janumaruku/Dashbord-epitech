services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: dashboard-backend
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
    restart: unless-stopped
